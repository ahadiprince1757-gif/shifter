# Local development environment — loaded by Vite only during `npm run dev`.\n# This file is safe to commit (no secrets, only localhost URLs).\n# Do NOT put Supabase keys here — use .env.local (git-ignored) for secrets.\nVITE_API_URL=http://localhost:3001
